export const LoadingSpinner = () => (
  /**
   * @Task
   *      Make this circle spin
   *      using css animations
   *
   */
  <Circle />
);

export const Circle = () => {
  <div>
    {/**
     * @Task Create a circle
     */}
  </div>;
};
