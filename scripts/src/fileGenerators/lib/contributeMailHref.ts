import { contributeEmailAddress } from "./contributeEmailAddress";

// export const mailHref = `mailto:${contributeEmailAddress}?subject=${encodeURIComponent(
//   contributeSubject
// )}&body=${encodeURIComponent(contributeMail)}`;

export const contributeMailHref = `mailto:${contributeEmailAddress}`;
